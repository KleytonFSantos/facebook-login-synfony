<div>
    <span
            class="absolute text-white text-4xl top-5 left-4 cursor-pointer"
            onclick="openSidebar()"
    >
      <i class="bi bi-filter-left px-2 bg-gray-900 rounded-md"></i>
    </span>
    <div
            class="sidebar fixed top-0 bottom-0 lg:left-0 p-2 w-[300px] overflow-y-auto text-center bg-gray-900"
    >
        {{ component('Sidebar:Header') }}
        {{ component('Sidebar:Search') }}

        {{ component('Sidebar:Menu', { title: 'Home', path: '', icon: 'bi bi-house-door-fill' }) }}
        {{ component('Sidebar:Menu', { title: 'Bookmark', path: '', icon: 'bi bi-bookmark-fill' }) }}

        <div class="my-4 bg-gray-600 h-[1px]"></div>

        {{
            component(
                'Sidebar:Dropdown',
                {
                    title: 'Categories',
                    dropdownMenu: [
                        component('Sidebar:Menu', { title: 'Bookmark', path: '', icon: 'bi bi-bookmark-fill' }),
                        component('Sidebar:Menu', { title: 'Home', path: '', icon: 'bi bi-house-door-fill' })
                    ]
                }
            )
        }}
        {{ component('Sidebar:Menu', { title: 'Logout', path: path('app_logout'), icon: 'bi bi-box-arrow-in-right' }) }}
    </div>
</div>